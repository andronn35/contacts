# Contacts

### How to run?

##### Locally:
- `npm i`
- `npm run dev`

##### Docker container:
- open command prompt
- go to the root directory
- `docker build -t сontacts`
- `docker run -it -p 3000:3000 сontacts`

